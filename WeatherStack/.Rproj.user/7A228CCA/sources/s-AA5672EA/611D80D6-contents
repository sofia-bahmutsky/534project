\name{Historical Weather Retrival}
\alias{Historical Weather}
%- Also NEED an '\alias' for EACH other topic documented here.
\title{
%%  ~~Historical weather retrival ~~
}
\description{
%%  ~~ The historical weather retrival function can look up historical weather data back to 2008 for worldwide locations. Pass one date (later than July 2008) or multiple semicolon-separated dates. Enter locations as city names, coordinates (lat/long), IP address, or ZIP code.  ~~
}
\usage{
Historical Weather(x)
}
%- Historical weather data retrieval.
\arguments{
  \item{x}{
%%     ~~ \code{function(key2, location, date, unit)} takes arguments API key, location (city names, coordinates (lat/long), IP address, or ZIP code), date (pass one historical date or multiple semicolon-separated dates to the API. (Example: 2015-01-21 for a single date or 2015-01-21;2015-01-22 for multiple dates)), unit (farenheit, celcius). ~~
}
\references{
%% ~https://weatherstack.com/documentation#jsonp_callbacks ~
}
\author(s){
%%  ~~Yuening Li, Sofia Bahmutsky~~
}
\examples{
##---- Should be DIRECTLY executable !! ----
##-- ==>  Define data, use random,
##--	or do  help(data=index)  for the standard data sets.

## The function is currently defined as
function (x)
{
  }
}
% Add one or more standard keywords, see file 'KEYWORDS' in the
% R documentation directory.
\keyword{ ~kwd1 }% use one of  RShowDoc("KEYWORDS")
\keyword{ ~kwd2 }% __ONLY ONE__ keyword per line
